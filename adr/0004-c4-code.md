# 4. C4 Code: текущий и проектируемый код CHT ToolBench

Date: 2026-06-14

## Status

Accepted

## Context

Structurizr не поддерживает C4 Code diagram как нативный view. Эта диаграмма фиксирует структуру существующего Python-кода и отделяет её от проектируемых классов моделирования и отчётности.

## Decision

```plantuml
@startuml
title C4 Level 4 — Code: CHT ToolBench
left to right direction
hide empty members
skinparam shadowing false
skinparam packageStyle rectangle
skinparam class {
  BackgroundColor #FFFFFF
  BorderColor #455A64
  ArrowColor #546E7A
  BackgroundColor<<Implemented>> #E8F5E9
  BorderColor<<Implemented>> #2E7D32
  BackgroundColor<<Stub>> #FFF8E1
  BorderColor<<Stub>> #F9A825
  BackgroundColor<<Planned>> #ECEFF1
  BorderColor<<Planned>> #78909C
  BackgroundColor<<Unused>> #F5F5F5
  BorderColor<<Unused>> #9E9E9E
}

package "Workbench entry point" {
  class CHTWorkbench <<Implemented>> {
    +Initialize()
    +Activated()
  }
}

package "toolbar.py" {
  abstract class _BaseCommand <<Implemented>> {
    +GetResources()
    +Activated()
    +IsActive()
  }

  class CreateProcessCommand <<Implemented>> {
    +Activated()
  }

  class AddStageCommand <<Implemented>> {
    +Activated()
  }

  class ValidateCommand <<Implemented>> {
    +Activated()
    +IsActive()
  }

  class RunSimulationCommand <<Stub>> {
    +Activated()
  }

  class ReportCommand <<Stub>>

  class "toolbar module" as ToolbarModule <<Implemented>> {
    +register_commands()
    -_select_stage_type()
    -_show_action_feedback()
  }

  _BaseCommand <|-- CreateProcessCommand
  _BaseCommand <|-- AddStageCommand
  _BaseCommand <|-- ValidateCommand
  _BaseCommand <|-- RunSimulationCommand
  _BaseCommand <|-- ReportCommand
}

package "tree.py" {
  class StageProxy <<Implemented>> {
    +attach(obj)
    +onDocumentRestored(obj)
  }

  class StageViewProvider <<Implemented>> {
    +claimChildren()
    +onDelete(view_object, subelements)
  }

  class "Process Tree Manager" as TreeModule <<Implemented>> {
    {static} STAGE_LIBRARY
    {static} DEFAULT_STAGE_ORDER
    +ensure_process_group(document)
    +ensure_default_process_tree(document)
    +add_stage_to_process(stage_name, document)
    +create_stage(document, process, stage_name)
    +upgrade_process_objects(document)
    +get_stage_names()
  }
}

package "commands/validate_process.py" {
  class ValidationResultsDialog <<Implemented>> {
    +exec()
    -_show_message(row)
    -_select_reference(message)
  }

  class "Validation Command Facade" as ValidationFacade <<Implemented>> {
    +run_validation(document, selection, show_dialog)
    +simulation_allowed(document, selection)
  }
}

package "rule_engine" {
  class ValidationEngine <<Implemented>> {
    +validate(document, selection): ValidationResult
    +can_simulate(document, selection)
    -_check_process(...)
    -_check_stage(...)
    -_check_material_and_targets(...)
    -_check_geometry(...)
  }

  class RuleBook <<Implemented>> {
    +strictness
    +severity(value, critical): Severity
    +rule_enabled(section, rule_name)
    +message(key, context, defaults)
  }

  class "Rules Loader" as RulesModule <<Implemented>> {
    +load_rules(path): RuleBook
    +default_rules_path()
  }

  enum Severity <<Implemented>> {
    INFO
    WARNING
    ERROR
  }

  enum ValidationStatus <<Implemented>> {
    VALID
    VALID_WITH_WARNINGS
    INVALID
    VALIDATION_FAILED
  }

  class ValidationMessage <<Implemented>> {
    +code: str
    +severity: Severity
    +problem: str
    +cause: str
    +recommendation: str
    +object_reference
  }

  class GeometryFacts <<Implemented>> {
    +dimensions_mm
    +volume_mm3
    +surface_area_mm2
    +minimum_thickness_mm
    +sharp_edges
    +hole_faces
    +risk indicators
  }

  class ValidationResult <<Implemented>> {
    +status: ValidationStatus
    +messages: List
    +geometry: GeometryFacts
    +fingerprint: str
    +error_count
    +warning_count
    +info_count
  }

  class "FreeCAD Adapter" as FreeCADAdapter <<Implemented>> {
    +find_process(document)
    +resolve_model(document, selection, process)
    +resolve_material(process, model, policy)
    +collect_stages(process)
    +compute_fingerprint(model, process, rules_text)
    +persist_result(document, result, model)
    +update_highlights(document, result, model)
    +read_saved_status(document)
  }

  class "Geometry Analyzer" as GeometryModule <<Implemented>> {
    +analyze_geometry(model, geometry_rules)
    -_validate_shape(shape)
    -_minimum_face_distance(faces, max_pairs)
  }

  class RuleConfigurationError <<Implemented>>
  class InvalidGeometryError <<Implemented>>

  ValidationResult "1" *-- "*" ValidationMessage
  ValidationResult "0..1" *-- GeometryFacts
  ValidationMessage --> Severity
  ValidationResult --> ValidationStatus
  RulesModule --> RuleBook
  RulesModule ..> RuleConfigurationError
  GeometryModule ..> InvalidGeometryError
}

package "Planned simulation" {
  class SimulationEngine <<Planned>> {
    +simulate(model, process, material): SimulationResult
  }
  class SimulationResult <<Planned>> {
    +case_depth
    +surface_hardness
    +core_hardness
    +risk_zones
    +time_series
  }
  class SimulationVisualizationAdapter <<Planned>> {
    +show_color_map(result)
    +show_legend()
    +reset()
  }
}

package "Planned reporting" {
  class ReportBuilder <<Planned>> {
    +build(simulation_result): ReportModel
  }
  class ChartFactory <<Planned>> {
    +carbon_profile()
    +hardness_profile()
    +temperature_profile()
    +case_depth_timeline()
  }
  class ReportWindow <<Planned>>
  class HtmlExporter <<Planned>>
}

package "domain/ skeleton" {
  class Process <<Unused>>
  class Stage <<Unused>>
  class CementationStage <<Unused>>
  Stage <|-- CementationStage
  Process "1" o-- "*" Stage
}

CHTWorkbench --> ToolbarModule : register_commands()
CHTWorkbench --> TreeModule : upgrade_process_objects()
ToolbarModule ..> CreateProcessCommand
ToolbarModule ..> AddStageCommand
ToolbarModule ..> ValidateCommand
ToolbarModule ..> RunSimulationCommand
ToolbarModule ..> ReportCommand
CreateProcessCommand --> TreeModule : ensure_default_process_tree()
AddStageCommand --> TreeModule : get_stage_names()\nadd_stage_to_process()
ValidateCommand --> ValidationFacade : run_validation()
ValidationFacade --> TreeModule : upgrade_process_objects()
ValidationFacade --> ValidationEngine : validate()\ncan_simulate()
ValidationFacade --> ValidationResultsDialog
ValidationEngine --> RulesModule
ValidationEngine --> FreeCADAdapter
ValidationEngine --> GeometryModule
ValidationEngine --> ValidationResult
TreeModule ..> StageProxy
TreeModule ..> StageViewProvider
RunSimulationCommand --> ValidationFacade : simulation_allowed()
RunSimulationCommand ..[#9E9E9E,dashed]> SimulationEngine : planned delegation
SimulationEngine ..[#9E9E9E,dashed]> SimulationResult
SimulationEngine ..[#9E9E9E,dashed]> SimulationVisualizationAdapter
ReportCommand ..[#9E9E9E,dashed]> ReportBuilder : planned delegation
ReportBuilder ..[#9E9E9E,dashed]> ChartFactory
ReportBuilder ..[#9E9E9E,dashed]> ReportWindow
ReportBuilder ..[#9E9E9E,dashed]> HtmlExporter

note bottom of TreeModule
  Фактические значения этапов по умолчанию
  находятся в STAGE_LIBRARY, а не в rules.yaml.
  get_active_document() автоматически создаёт документ.
  ensure_process_group() автоматически создаёт процесс.
end note

note bottom of RunSimulationCommand
  Реализована только проверка актуальной
  успешной валидации и сообщение-заглушка.
end note

note bottom of ReportCommand
  Команда зарегистрирована, но использует
  базовый Activated() и показывает заглушку.
end note

note bottom of Process
  Каркас domain/ не участвует в текущем
  потоке создания процесса или валидации.
end note

legend right
  |= Цвет |= Статус |
  |<#E8F5E9> зелёный | Implemented |
  |<#FFF8E1> жёлтый | Stub |
  |<#ECEFF1> серый | Planned |
  |<#F5F5F5> светло-серый | неиспользуемый каркас |
endlegend
@enduml
```

## Consequences

Диаграмма следует фактическим импортам и вызовам. Проектируемые классы моделирования и отчётности не существуют в репозитории и показаны только как возможные границы будущего кода. Текущие `domain.Process`, `domain.Stage` и `domain.CementationStage` не интегрированы с объектами FreeCAD из `tree.py`.
